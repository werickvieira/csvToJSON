import service from '../src/service';

describe('function service', () => {
  const _ = service();
  it('should failed when detected have an empty argument', () => {
    // expect(m.detectSeparator()).toHaveBeenCalled();
  });
});
